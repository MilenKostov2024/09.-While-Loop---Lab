while (true)
{
    string input = Console.ReadLine();

    if (input == "Stop")
    {
        break; // Спира изпълнението на цикъла при команда "Stop"
    }

    Console.WriteLine(input);
}